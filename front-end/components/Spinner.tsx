// components/Spinner.js

import styles from '../styles/Spinner.module.css'; // Import the spinner styles

export default function Spinner() {
  return (
    <div className={styles.loader}></div>
  );
}
